@model HeaderDto


<header class="header_section">
    <div class="container-fluid">
        <nav class="navbar navbar-expand-lg custom_nav-container">
            <a class="navbar-brand" href="index.html">
                <span>
                    Connex
                </span>
            </a>

            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class=""> </span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav" style="align-items:center;  font-family: 'Noto Sans', sans-serif;">
                    <li class="nav-item active">
                        <a class="nav-link" id="headerHome" href="index.html">Əsas səhifə <span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" id="headerAbout" href="about.html"> Haqqımızda</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" id="headerServices" href="service.html">Xidmətlər</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" id="headerProjects" href="why.html">Layihələr</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" id="headerContact" href="team.html">Əlaqə</a>
                    </li>
                    <li class="nav-item">
                        <div class="dropdown">
                            <button class="dropbtn">
                                <img src="@Model.SelectedLanguage.ImagePath" alt="@Model.SelectedLanguage.Code" class="flag-icon">
                            </button>
                            <div class="dropdown-content">
                                @foreach (var language in Model.Languages)
                                {
                                    <a style="@(language.Id==Model.SelectedLanguage.Id ? "background-color:#180c65" : "") " href="?lang=@language.Code"><img src="@language.ImagePath" alt="@language.Code" class="flag-icon"></a>
                                }

                            </div>
                        </div>
                    </li>
                   
                </ul>
            </div>
        </nav>
    </div>
</header>
